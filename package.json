{
  "name": "silvus-me",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo portfolio dev + broadcast",
  "packageManager": "npm@11.4.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @silvus/api & npm run dev -w @silvus/web",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start -w @silvus/api & npm run start -w @silvus/web",
    "prod": "npm run build && npm run start",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
